// import dotenv from "dotenv";

// dotenv.config();

let config = {
  api_host: "https://server-semina-v3.herokuapp.com/",
  api_image: "https://server-semina-v3.herokuapp.com/uploads/",
};
// let config = {
//   api_host: "http://localhost:4000",
//   api_image: "http://localhost:4000/uploads",
// };

export { config };
